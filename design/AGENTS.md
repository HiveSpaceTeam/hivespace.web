@C:\Users\sunsh\.codex\RTK.md

# Project Rules

- Do not create redundant snapshot, duplicate, or throwaway files unless the user explicitly asks for a versioned copy.
- Edit the canonical project file in place whenever safe to do so.
- Use temporary files only when required to complete the task, and delete them before handing the task back.
- Before finishing any task, remove unreferenced temp files, import leftovers, and duplicate artifacts created during the work.
- If a temporary or backup file must be kept, ask the user first and explain why it is needed.
